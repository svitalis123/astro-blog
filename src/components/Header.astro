---
import Link from "./Link.astro";
const {homelink} = Astro.props;
---

<header>
  <h1>
    The Header 
  </h1>
  <div>
    {
		homelink.map((sn) => (
        <Link links={sn} />
    ))
    }
  </div>
</header>